apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-dns-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: nslookup
    image: alpine
    command: ["nslookup", "theguardian.com", "{{ template "pihole.fullname" . }}-udp"]
  restartPolicy: Never
